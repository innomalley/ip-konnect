# Base URL of the Python REST API. Leave blank while running on mock data.
VITE_API_BASE_URL=http://localhost:8000/api

# Toggle the mock backend. Set to "false" once the Python API is wired up.
VITE_USE_MOCK=true
